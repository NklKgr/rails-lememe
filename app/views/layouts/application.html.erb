<!DOCTYPE html>
<html>
  <head>
    <head>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path('fav.png') %>
  </head>

  <body class="pages_container">
    <div>
      <%= render "layouts/navbar" %>
      <%= render "shared/flashes" %>
      <%= yield %>
    </div>
  </body>

</html>
